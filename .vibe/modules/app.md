# Module: app

**Role:** Bootstraps the page: mounts the upload UI (title, description, file input) into the `#app` root.
**Files:** `src/main.ts`, `src/style.css`
**Exports:** none (entry point, run for its side effect of populating `#app`)
**Depends on:** none
