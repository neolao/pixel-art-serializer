# Module: app

**Role:** Bootstraps the page: renders the upload UI (title, description, file input, preview image, error message) into `#app`, and wires the file input's `change` event to the upload module.
**Files:** `src/main.ts`, `src/style.css`
**Exports:** none (entry point, run for its side effect of populating `#app`)
**Depends on:** `modules/upload.md`
