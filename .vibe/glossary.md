# Ubiquitous Language

No domain concepts are implemented yet — the codebase only has an app entry point (`src/main.ts`). This glossary will be populated once the pixel-size detection and color-indexing logic exists.
